name: remote_data
description: A new Flutter package project.
version: 0.0.1
publish_to: none
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  bloc:
  blop: 
    path: ../blop
  bloc_utils:
    path: ../bloc_utils

  # for persistent store
  hive: ^2.0.0
  # hive_flutter: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: 

  blop_generator: 
    path: ../blop_generator
  
  build_runner: ^2.0.0