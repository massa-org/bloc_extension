class BlopProcess {
  final String? name;

  const BlopProcess({this.name});
}

const blopProcess = BlopProcess();

class BlopProcessor {
  const BlopProcessor();
}

const blopProcessor = BlopProcessor();
