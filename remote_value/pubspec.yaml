name: remote_value
description: Package that implement remote value abstraction and add cubit to mange it.
version: 0.0.3
homepage: https://github.com/massa-org/massa.flutter

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  bloc: ^7.0.0
  blop: ^0.0.2
  massa_utils: ^0.0.2

  # for persistent store
  hive: ^2.0.0
  freezed_annotation: ^0.14.2
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.1

  blop_generator: ^0.0.2
  freezed: ^0.14.2
  
  
  build_runner: ^2.0.0