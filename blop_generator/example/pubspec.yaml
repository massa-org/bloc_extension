name: example
description: A new Flutter package project.
version: 0.0.1
homepage:

publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  
  bloc:
  blop: 
  freezed_annotation:
  bloc_utils:
    path: ../../bloc_utils

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  build_runner: 

  freezed: 
  blop_generator: 
    path: ../

dependency_overrides:
  
  blop: 
    path: ../../blop